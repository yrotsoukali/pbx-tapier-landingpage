# pbx-tapier

## Customer logos

Add or remove image files in the `logos` folder, then run:

```powershell
.\build.ps1
```

The script regenerates `logos-manifest.js`; `index.html` does not need to be edited.
